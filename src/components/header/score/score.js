import React from 'react';
import classes from './score.module.css';

const Score = (props) => {

    return (
        <h5 className={classes.ScoreContainer}>Score: 11</h5>
    );
};

export default Score;
