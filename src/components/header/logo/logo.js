import React from 'react';
import classes from './logo.module.css';

const Logo = () => (
  <h1>
    <span className={classes.FirstHalf}>Song</span>
    <span className={classes.SecondHalf}>bird</span>
  </h1>
);

export default Logo;
